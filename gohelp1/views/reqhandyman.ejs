<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GoHelp</title>
  </head>
  <style>
    body {
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        background-color: #fff;
    }

    .form-container {
        position: relative;
        width: 100%;
        max-width: 800px;
        padding: 40px;
        background: white;
        border: 2px solid #0066cc;
        border-radius: 5px;
        overflow: hidden;
    }

    .corner-decoration {
        position: absolute;
        width: 150px;
        height: 150px;
        background: #ff9933;
    }

    .top-left {
        top: 0;
        left: 0;
        clip-path: polygon(0 0, 0% 100%, 100% 0);
    }

    .bottom-right {
        bottom: 0;
        right: 0;
        clip-path: polygon(100% 100%, 0% 100%, 100% 0);
    }

    .form-title {
        text-align: center;
        margin-bottom: 30px;
    }

    .form-title h2 {
        display: inline-block;
        background: #ff9933;
        padding: 10px 20px;
        border-radius: 25px;
        color: black;
        font-size: 18px;
        margin: 0;
    }

    .form-group {
        display: flex;
        margin-bottom: 20px;
        align-items: flex-start;
    }

    .form-label {
        background: #ff9933;
        padding: 5px 15px;
        margin-right: 10px;
        min-width: 100px;
        text-align: left;
        font-weight: bold;
    }

    .form-input {
        flex: 1;
        padding: 8px 15px;
        border: none;
        background: #e0e0e0;
        width: 100%;
    }

    textarea.form-input {
        height: 100px;
        resize: none;
    }

    .submit-btn {
        display: block;
        margin: 30px auto 0;
        background: #ff9933;
        border: none;
        padding: 10px 30px;
        font-weight: bold;
        cursor: pointer;
        border-radius: 25px;
    }

    .submit-btn:hover {
        background: #ff8800;
    }
    @media screen and (max-width: 768px) {
    .form-container {
        padding: 30px 20px;
    }

    .corner-decoration {
        width: 100px;
        height: 100px;
    }

    .form-title h2 {
        font-size: 16px;
        padding: 8px 15px;
    }
}

@media screen and (max-width: 576px) {
    .form-group {
        flex-direction: column;
    }

    .form-label {
        margin-right: 0;
        margin-bottom: 5px;
        width: fit-content;
    }

    .form-input {
        width: 100%;
    }

    .corner-decoration {
        width: 80px;
        height: 80px;
    }

    .form-container {
        padding: 20px 15px;
    }

    .form-title h2 {
        font-size: 14px;
        padding: 6px 12px;
    }

    .submit-btn {
        width: 100%;
        padding: 12px;
    }
}

/* For very small screens */
@media screen and (max-width: 320px) {
    .form-title h2 {
        font-size: 12px;
    }

    .form-label {
        font-size: 14px;
    }

    .form-input {
        font-size: 14px;
    }

    .corner-decoration {
        width: 60px;
        height: 60px;
    }
}

/* For high-DPI screens */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
    .form-container {
        border-width: 1px;
    }
}

/* For landscape orientation on mobile devices */
@media screen and (max-height: 500px) and (orientation: landscape) {
    body {
        align-items: flex-start;
    }

    .form-container {
        margin: 20px auto;
    }

    .corner-decoration {
        display: none;
    }
}

/* For print media */
@media print {
    .form-container {
        border: 1px solid #000;
    }

    .corner-decoration {
        display: none;
    }

    .submit-btn {
        display: none;
    }
}

/* Ensure form inputs are accessible on touch devices */
@media (hover: none) and (pointer: coarse) {
    .form-input {
        padding: 12px 15px;
    }

    .submit-btn {
        padding: 15px 30px;
    }
}
</style>
  <body>
    <!-- <form action="/main/<%= username %>/<%=emailaddress%>" method="post">
      <input type="text" placeholder="enter Fullname" name="usernames"/>
      <input type="text" placeholder="address" name="useraddress"/>
      <input type="number" placeholder="mobile" name="usermobile"/>
      <input type="email" placeholder="email" name="useremail"/>
      <input type="text" placeholder="service" value="HandimanService" />
      <input type="text" placeholder="enter particular service" name="userservice">
      
      <button>submit</button>
    </form> -->
    <div class="form-container">
      <div class="corner-decoration top-left"></div>
      <div class="corner-decoration bottom-right"></div>
      
      <div class="form-title">
          <h2>Fill Your Requested Service Details</h2>
      </div>

      <form id="serviceForm" action="/main/<%= username %>/<%=emailaddress%>" method="post">
          <div class="form-group">
              <label class="form-label">Name :</label>
              <input type="text" class="form-input" placeholder="Enter your full name" name="usernames" required>
          </div>

          <div class="form-group">
              <label class="form-label">Address :</label>
              <textarea class="form-input" placeholder="Enter Your Valid Address" name="useraddress" required></textarea>
          </div>

          <div class="form-group">
              <label class="form-label">Mobile :</label>
              <input type="tel" class="form-input" placeholder="Enter Your Mobile Number" name="usermobile" required>
          </div>

          <div class="form-group">
              <label class="form-label">Email :</label>
              <input type="email" class="form-input" placeholder="Enter Your Email" name="useremail" required>
          </div>

          <div class="form-group">
              <label class="form-label">service</label>
              <input type="text" class="form-input" placeholder="Enter A service required" value="<%=category%> service" required>
          </div>
          <div class="form-group">
            <label class="form-label">Describe Service:</label>
            <input type="text" class="form-input" placeholder="Enter A specific service required" name="userservice" required>
        </div>

          <button type="submit" class="submit-btn">Request A Service ➤</button>
      </form>
  </div>

  <script>
      function validateForm(event) {
          event.preventDefault();
          
          // Get all form inputs
          const inputs = document.querySelectorAll('.form-input');
          let isValid = true;
          
          // Basic validation
          inputs.forEach(input => {
              if (!input.value.trim()) {
                  isValid = false;
                  input.style.backgroundColor = '#ffe0e0';
              } else {
                  input.style.backgroundColor = '#e0e0e0';
              }
          });

          // Mobile number validation
          const mobileInput = document.querySelector('input[type="tel"]');
          const mobileRegex = /^\d{10}$/;
          if (!mobileRegex.test(mobileInput.value.trim())) {
              isValid = false;
              mobileInput.style.backgroundColor = '#ffe0e0';
              alert('Please enter a valid 10-digit mobile number');
              return false;
          }

          // Email validation
          const emailInput = document.querySelector('input[type="email"]');
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if (!emailRegex.test(emailInput.value.trim())) {
              isValid = false;
              emailInput.style.backgroundColor = '#ffe0e0';
              alert('Please enter a valid email address');
              return false;
          }

          if (isValid) {
              alert('Form submitted successfully!');
              event.target.reset();
          }

          return false;
      }
  </script>
  </body>
</html>
