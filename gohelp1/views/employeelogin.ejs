<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GoHelp</title>
</head>
<style>
  body {
      font-family: Arial, sans-serif;
      background-color: #fdf6e9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
  }
  .login-card {
      background-color: white;
      border-radius: 32px;
      padding: 32px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 800px;
      display: flex;
  }
  .login-form {
      flex: 1;
      padding-right: 32px;
  }
  .login-image {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
  }
  h1 {
      text-align: center;
      margin-bottom: 32px;
  }
  .login-title {
      background-color: #f97316;
      color: white;
      padding: 8px 24px;
      border-radius: 9999px;
  }
  .input-group {
      margin-bottom: 16px;
  }
  label {
      display: block;
      margin-bottom: 8px;
      font-size: 14px;
  }
  input[type="email"],
  input[type="password"] {
      width: 100%;
      padding: 8px;
      border: none;
      background-color: #f3f4f6;
      border-radius: 4px;
  }
  .remember-me {
      display: flex;
      align-items: center;
      margin-bottom: 16px;
  }
  .remember-me input {
      margin-right: 8px;
  }
  .login-button {
      background-color: #f97316;
      color: white;
      border: none;
      padding: 12px;
      width: 100%;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
  }
  .login-button:hover {
      background-color: #ea580c;
  }
  .links {
      display: flex;
      justify-content: space-between;
      margin-top: 16px;
      font-size: 14px;
  }
  .links a {
      color: #4b5563;
      text-decoration: none;
  }
  .links a:hover {
      text-decoration: underline;
  }
  @media (max-width: 768px) {
  .login-card {
    flex-direction: column;
    position: relative; /* Change from absolute to relative */
    max-width: 90%; /* Make the width dynamic for different screen sizes */
    margin: 70px auto 0 auto; /* Center horizontally and adjust top spacing */
    padding: 15px; /* Add some padding for better spacing */
    box-sizing: border-box; /* Ensure padding doesn't affect width */
  }

  .login-image {
    order: -1;
    margin-bottom: 20px;
    text-align: center; /* Center the image */
  }

  .login-image img {
    max-width: 90%; /* Dynamically adjust image size */
    height: auto; /* Maintain aspect ratio */
  }
  .login-title{
    font-size: 25px;
  }
}

  
  img {
    border-radius:30px;
  }
</style>
<body>
  <form action="/employeelogin" method="post">
    <div class="login-card">
      <div class="login-form">
          <h1><span class="login-title">Employee Login</span></h1>
          <% if (error_msg.length > 0) { %>
            <div style="color: red;"><%= error_msg %></div>
        <% } %>
        <% if (success_msg.length > 0) { %>
            <div style="color: green;"><%= success_msg %></div>
        <% } %>
              <div class="input-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="employmail" placeholder="Enter Your Email" required>
              </div>
              <div class="input-group">
                  <label for="password">Password</label>
                  <input type="password" id="password" name="employpassword" placeholder="Enter your password" required>
              </div>
              <div class="remember-me">
                  <input type="checkbox" id="remember" name="remember">
                  <label for="remember">Remember Me</label>
              </div>
              <button  class="login-button">Login</button>
          
          <div class="links">
              <a href="/forget">Forgot Password</a>
              <a href="/employeesignup">New to GoHelp ? Create an Account</a>
          </div>
      </div>
      <div class="login-image">
        <img src="/images/Rectangle 16.png" alt="Service Illustration" width="300" height="300" >
      </div>
  </div>
  </form>

 
  
</body>
</html>